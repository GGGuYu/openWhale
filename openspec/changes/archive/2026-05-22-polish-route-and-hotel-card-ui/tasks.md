## 1. 路线卡片重设计

- [x] 1.1 将 `SecondaryScrollableTabRow` 替换为等宽 `Row` + 图标标签 chip 布局，4 个出行方式在一屏内完整可见
- [x] 1.2 重设计 `RouteModePanel`：废除 `StatBadge` 堆叠，改用左侧大号耗时 + 右侧图标距离 + 出行方式名称的视觉层次布局
- [x] 1.3 保留地图图片占位区域

## 2. 酒店列表卡片打磨

- [x] 2.1 优化 `PlatformQuoteRow`：最低价平台使用强调色 + 加粗字重
- [x] 2.2 优化 `HotelRow` 内部间距和信息层级（酒店名 > 距离摘要 > 平台报价）

## 3. 验证

- [x] 3.1 在手机上验证路线卡片 4 个 tab 一屏可见，无需横向滑动
- [x] 3.2 在手机上验证酒店列表卡片价格对比清晰可辨
- [x] 3.3 确保选项卡片视觉效果不变（回归检查）
